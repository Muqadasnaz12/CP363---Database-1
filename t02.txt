SELECT
m.memberSurname,
m.memberForename,

(SELECT COUNT(*)
FROM memberBroad mb
WHERE mb.memberBroadMemberId = m.memberID) AS broadCount,
(SELECT COUNT(*)
FROM memberNarrow mn
WHERE mn.memberNarrowMemberId = m.memberID) AS narrowCount

FROM member m
ORDER BY
m.memberSurname,
m.memberForename