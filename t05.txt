SELECT
m.memberSurname,
m.memberForename,

(SELECT COUNT(*)
FROM pub p
WHERE p.pubMemberId = m.memberID) AS pubCount

FROM member m

WHERE
(SELECT COUNT(*)
FROM pub p
WHERE p.pubMemberId = m.memberID) >= 5

ORDER BY
m.memberSurname,
m.memberForename