SELECT
m.memberSurname,
m.memberForename,

(SELECT COUNT(*)
FROM memberBroad mb
WHERE mb.memberBroadMemberId = m.memberID) AS broadCount

FROM member m

WHERE
(SELECT COUNT(*)
FROM memberBroad mb
WHERE mb.memberBroadMemberId = m.memberID) >= 9

ORDER BY
m.memberSurname,
m.memberForename