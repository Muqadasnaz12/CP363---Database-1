SELECT
m.memberSurname,
m.memberForename,
b.broadDesc

FROM member m
JOIN memberBroad mb
ON m.memberID = mb.memberBroadMemberId
JOIN broad b
ON mb.memberBroadBroadId = b.broadID

WHERE m.memberID NOT IN
(SELECT pubMemberId
FROM pub)

ORDER BY
m.memberSurname,
m.memberForename,
b.broadDesc